[package]
name = "proton-informer-helper-protocol"
version = "0.1.1"
edition = "2024"
rust-version = "1.96"
license = "MIT"
description = "Shared JSON protocol for ProtonInformer Wine helpers"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
thiserror = "2.0.18"

[dev-dependencies]
serde_json = "1.0.150"
